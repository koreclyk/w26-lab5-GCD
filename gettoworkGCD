// fleshing out the program

/*
USER INPUT: Chapter 5
*Scanner
*Logical operators
*a and b
*validate input/exit if invalid

LOOPS: Chapter 7
*while loops / nested loops
*updating variables inside loops
*repeat division by 2
*loop until a==b

Operators: Chapter 2
*% to check even/odd
*/ division
*assignment operators
>>> a % 2 == 0 (checking if a is even)
>>> a = a / 2 (does the division)

Methods: Chapter 6
*public static int gcd(int a, int b)

Debugging and Design: Chapter 3
*Variable values
*testing
*avoid infinite loops (outside of personal time)

GCD CALCS:
if a % 2 == 0 && b % 2 == 0

*/

import java.util.Scanner;

class bIGCD {
   public static void main(String[] args) { // if anything larger than an int is input, there will be an error
      Scanner input = new Scanner(System.in);

      System.out.print("enter a: ");
      int a = input.nextInt();
      System.out.print("enter b: ");
      int b = input.nextInt();
      
      if (a <= 0 || b <= 0) {
         System.out.println("THIS AINT IT!");
         return; // BREAK - exits loop or switch while RETURN exits the method itself to stop the logic from running regardless of the answer
      }



      int d = 0;

      while (a% 2 == 0 && b % 2 == 0){
         a /= 2;
         b /= 2;
         d++;
      }
      
      
      while (a != b){
         if (a > b){
            a = a - b;
            while (a % 2 == 0) {
               a /= 2;
            }
         } else {
            b = b - a;
            while (b % 2 == 0){
               b /= 2;
            }
         }
      }
      
      
      
      double calcbIGCD = Math.pow(2, d) * a;
      System.out.printf("The GCD is: %.1f", calcbIGCD);
   }
}
